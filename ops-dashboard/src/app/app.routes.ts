import { Routes } from '@angular/router';
import { TradeDashboard } from './components/trade-dashboard/trade-dashboard';

export const routes: Routes = [
  { path: '', component: TradeDashboard },
];
